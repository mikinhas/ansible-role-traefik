---
- name: Create traefik config directory
  become: true
  ansible.builtin.file:
    path: "{{ traefik_config_dir }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Create traefik acme directory
  become: true
  ansible.builtin.file:
    path: "{{ traefik_config_dir }}/acme"
    state: directory
    owner: root
    group: root
    mode: "0700"
  when: traefik_acme_email | length > 0

- name: Configure traefik static config
  become: true
  ansible.builtin.template:
    src: traefik.yml.j2
    dest: "{{ traefik_config_dir }}/traefik.yml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart traefik

- name: Run traefik container
  community.docker.docker_container:
    name: "{{ traefik_container_name }}"
    image: "traefik:{{ traefik_version }}"
    state: started
    restart_policy: unless-stopped
    env: >-
      {{
        ({'DOCKER_API_VERSION': '1.45'} if traefik_docker_provider else {})
        | combine(traefik_env)
      }}
    networks: "{{ traefik_networks | default(omit) }}"
    ports:
      - "80:80"
      - "443:443"
    volumes: >-
      {{
        [traefik_config_dir ~ '/traefik.yml:/etc/traefik/traefik.yml:ro']
        + (['/var/run/docker.sock:/var/run/docker.sock:ro'] if traefik_docker_provider else [])
        + ([traefik_config_dir ~ '/acme:/etc/traefik/acme'] if traefik_acme_email | length > 0 else [])
      }}
